# What is Computation?

Computation is about solving problems using computers.

Programming focuses on writing recipes that instruct a computer how to generate desired results.

---

# Declarative vs Imperative Knowledge

| Declarative Knowledge | Imperative Knowledge |
|------------------------|----------------------|
| Statements of fact | A recipe or "how-to" |
| Describes what is true | Describes how to accomplish something |

Programming is primarily about writing **recipes** that generate facts.

---

# Python Programs

A Python program is a sequence of **definitions** and **commands** that are evaluated and executed by the **Python interpreter**.

- **Definitions** are evaluated.
- **Commands (statements)** are executed.

## Commands

Commands instruct the interpreter to perform an action.

Commands can be:

- Typed directly into the Python shell.
- Stored in a Python file and executed by the interpreter.

> [!NOTE]
> Problem Set 0 introduces these concepts using **Anaconda**.

---

# 🐍 Important Python Notes

> [!IMPORTANT]
> ## Key Points
> 1. **Python is dynamically typed.**
>    - You **do not need to declare the data type** of a variable explicitly.
>    - Python automatically determines the type based on the assigned value.
>
> 2. **Python is case-sensitive.**
>    - Variable names with different letter cases are treated as different variables.
>    - Example:
>      ```python
>      age = 20
>      Age = 30
>      print(age)  # 20
>      print(Age)  # 30
>      ```
>
> 3. **Indentation is mandatory in Python.**
>    - Indentation defines code blocks (instead of braces `{}`).
>    - The standard convention is **4 spaces** per indentation level (PEP 8).
>    - Example:
>      ```python
>      if age >= 18:
>          print("Adult")
>      ```
>
> 4. **Keywords are reserved words.**
>    - Keywords have predefined meanings in Python and **cannot be used as variable names**.
>    - Examples:
>      ```text
>      if
>      for
>      while
>      else
>      class
>      in
>      True
>      False
>      None
>      def
>      return
>      import
>      ```

---

# Built-in Data Types

| Type | Description | Example |
|------|-------------|---------|
| `int` | Represents integers | `5`, `-100` |
| `float` | Represents real numbers | `3.27`, `2.0` |
| `bool` | Represents Boolean values | `True`, `False` |
| `NoneType` | Special type with only one value | `None` |

---

# Checking an Object's Type

Use the `type()` function to determine the type of an object.

```python
type(1234)
type(8.99)
type(True)
```

---
# 🔄 Type Conversion & Type Casting

## 1. Implicit Type Conversion (Automatic)
- Done **automatically by Python**.
- Occurs when compatible data types are used together.

```python
a = 5
b = 3.0
print(a + b)   # 8.0
```

> Python converts `int` to `float` automatically.

---

## 2. Type Casting (Explicit)
- Done **manually by the programmer** using built-in functions.

```python
x = 10

float(x)   # 10.0
str(x)     # "10"
```

### Common Type Casting Functions
- `int()`
- `float()`
- `str()`
- `bool()`
- `list()`
- `tuple()`

---

# Python Operators

Operators perform operations on objects and produce a new value.

## Arithmetic Operators

| Operator | Description | Example | Result |
|----------|-------------|---------|--------|
| `+` | Addition | `i + j` | Sum of `i` and `j` |
| `-` | Subtraction | `i - j` | Difference between `i` and `j` |
| `*` | Multiplication | `i * j` | Product of `i` and `j` |
| `/` | Division | `i / j` | Always returns a `float` |
| `//` | Floor Division | `i // j` | Quotient rounded down |
| `%` | Modulus | `i % j` | Remainder after division |
| `**` | Exponentiation | `i ** j` | `i` raised to the power `j` |

---

# ⚡ Operator Precedence in Python

Operator precedence determines the **order in which operations are evaluated**.

### Order of Precedence (Highest → Lowest)

1. `()` → Parentheses
2. `**` → Exponentiation
3. `+x`, `-x`, `~x` → Unary operators
4. `*`, `/`, `//`, `%` → Multiplication, Division, Floor Division, Modulus
5. `+`, `-` → Addition, Subtraction
6. `<<`, `>>` → Bitwise Shift
7. `&` → Bitwise AND
8. `^` → Bitwise XOR
9. `|` → Bitwise OR
10. `<`, `<=`, `>`, `>=`, `!=`, `==` → Comparison
11. `not` → Logical NOT
12. `and` → Logical AND
13. `or` → Logical OR
14. `=`, `+=`, `-=`, `*=`, ... → Assignment (Lowest)

### Example

```python
x = 10
y = 20
z = 5

print(x > z and y > x)   # True
print(x > y or y > z)    # True
print(not (x > y))       # True
```

> [!NOTE]
> **Unary operators** work on **a single operand** (a single value or variable).

### Common Unary Operators
- `+x` → Unary Plus (indicates a positive value)
- `-x` → Unary Minus (indicates a negative value)

### Example

```python
x = 10

print(+x)   # 10
print(-x)   # -10
```

---

# Naming Conventions

- You can write variables in python using 3 ways:
      - Camel case : sheryiansSchool
      - Pascal case : SheryiansSchool
      - Snake case : sheryians_school


[Next: 2. Strings, InputOutput, and Branching ➔](2.%20Strings,%20InputOutput,%20and%20Branching.md)